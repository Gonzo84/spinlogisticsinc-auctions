# Test-Fix Loop Summary

**Mode:** demo
**Started:** 2026-03-07
**Status:** Complete

## Iterations

4 reports processed across 4 iterations.

### Iteration 1 (earlier session)

| Bug ID | Severity | Component | Description | Files Changed |
|--------|----------|-----------|-------------|---------------|
| BUG-DEMO01 | Critical | auction-engine | Award endpoint missing (POST /auctions/{id}/award returns 404) | `AuctionResource.kt`, `casbin_policy.csv` |
| BUG-DEMO02 | Major | gateway + payment | PATCH proxy not supported in gateway; settle endpoint returns 405 | `ApiProxyResource.kt`, `casbin_policy.csv` (payment) |
| BUG-DEMO03 | Major | buyer-web | Bid button returns 403 for admin role without UI feedback | `BidPanel.vue`, 7 locale JSON files |
| BUG-DEMO04 | Major | keycloak | buyer2@test.com not configured in Keycloak | `auction-platform-realm.json` |
| BUG-DEMO05 | Minor | admin-dashboard | Payment amount shows NaN (backend field mismatch) | `usePayments.ts` |

### Iteration 2

| Bug ID | Severity | Component | Description | Files Changed |
|--------|----------|-----------|-------------|---------------|
| BUG-DEMO06 | Major | keycloak | buyer2@test.com realm not re-imported on restart | `docker/compose/infrastructure.yml` |
| BUG-DEMO07 | Major | payment-service | Checkout does not fetch hammer price from auction-engine | `AuctionLotLookupService.kt` (new), `PaymentResource.kt`, `application.yml`, `payment-service.yml`, `AuctionResource.kt` |
| BUG-DEMO08 | Major | payment-service | sellerId set to lotId instead of actual seller UUID | (same as BUG-DEMO07) |
| BUG-DEMO10 | Minor | auction-engine | Read model does not store winnerId/winningBid on close | `AuctionReadModelRepository.kt` |
| BUG-DEMO11 | Minor | shared/kotlin-commons | Auction creation 400 with empty body on validation failure | `ConstraintViolationExceptionMapper.kt` (new), `build.gradle.kts` |
| BUG-DEMO12 | Info | payment-service | Admin payments table empty Lot/Buyer/Seller columns | `CheckoutService.kt` |

### Iteration 3

| Bug ID | Severity | Component | Description | Fix Applied | Files Changed |
|--------|----------|-----------|-------------|-------------|---------------|
| BUG-DEMO01 | Major | payment-service | Checkout hammerPrice=0 despite correct award — inter-service HTTP lookup fails silently | Added explicit `items` mode to `CheckoutRequest` so callers pass auctionId + hammerPrice directly, bypassing fragile HTTP lookups. Also added `fetchAuctionResultByAuctionId()` method. | `PaymentDtos.kt`, `PaymentResource.kt`, `AuctionLotLookupService.kt` |
| BUG-DEMO02 | Major | payment-service | Settlement endpoint fails (PATCH returns HTTP error) — rejects PROCESSING status payments | Allow admin settle for PROCESSING payments by auto-completing them first, then proceeding with settlement creation. | `PaymentResource.kt` |

### Iteration 4 (current)

| Bug ID | Severity | Component | Description | Fix Applied | Files Changed |
|--------|----------|-----------|-------------|-------------|---------------|
| BUG-DEMO02 | Minor | payment-service | Payment Oversight KPI cards show 0.00 EUR — summary endpoint excludes PROCESSING payments | Added PROCESSING status to pending totals in `getPaymentsSummary()` | `PaymentResource.kt` |
| INFO-DEMO02 | Info | payment-service, user-service, kotlin-commons | Outbox poller type mismatch (bigint = varchar) causes repeated re-publishing | Changed `setString` to `setLong` with `id.toLong()` in `markPublished()` | `PaymentOutboxPoller.kt`, `UserOutboxPoller.kt`, `OutboxPoller.kt` |

## Unfixable / Skipped

| Bug ID | Severity | Reason |
|--------|----------|--------|
| BUG-DEMO03 | Minor | PrimeVue Select not automatable — test tooling limitation, not a code bug |
| BUG-DEMO04 | Info | SSO session sharing — expected Keycloak behavior, not a code bug |
| (iter 1) BUG-DEMO09 | Minor | PrimeVue Select not automatable — test tooling limitation |
| (iter 1) BUG-DEMO13 | Info | SSO session sharing — expected Keycloak behavior |
| (iter 4) BUG-DEMO01 | Minor | SSO session sharing — Keycloak behavior, not a code bug |
| (iter 4) INFO-DEMO01 | Info | PrimeVue Select breaks Playwright accessibility tree — library behavior |

### Iteration 5 (2026-03-07)

Report: `demo-happy-path-report-2026-03-07.md`

| Bug ID | Severity | Component | Description | Fix Applied | Files Changed |
|--------|----------|-----------|-------------|-------------|---------------|
| BUG-D03 | Minor | buyer-web | `/watchlist` and `/account/watchlist` return 404 — page exists at `/my/watchlist` but no redirect aliases | Added `routeRules` redirects from `/watchlist` and `/account/watchlist` to `/my/watchlist` | `nuxt.config.ts` |

## Unfixable / Skipped (Iteration 5)

| Bug ID | Severity | Reason |
|--------|----------|--------|
| BUG-D01 | Major | Seller-service NATS sync gap — architectural limitation, no consumer for payment events |
| BUG-D02 | Minor | PrimeVue InputNumber not automatable — test tooling limitation |
| BUG-D04 | Info | PrimeVue button clicks not automatable — test tooling limitation |
| BUG-D05 | Info | Keycloak SSO session sharing — expected SSO behavior |

## Build Verification

- Backend: No backend files changed — no build needed
- Frontend (buyer-web): `npm run build` — BUILD SUCCESSFUL
